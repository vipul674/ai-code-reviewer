# RepoSage Frontend Environment Variables

# URL of the backend API service
VITE_API_URL=http://localhost:5000

# NOTE: Never put REPOSAGE_API_KEY in frontend env vars.
# Vite inlines VITE_ prefixed variables into the client bundle,
# exposing them to all site visitors. The API key is handled
# server-side via session cookies and prompts.
